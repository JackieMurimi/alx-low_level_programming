#include "main.h"

/**
 * _puts - prints a string, followed by a new line,
 * @str: pointer to the string
 *
 * Return: Always 0 (Success)
 */
void _puts(char *str)
{
	int i = 0;

	while
		(str[i]);
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
	return (0);
}
